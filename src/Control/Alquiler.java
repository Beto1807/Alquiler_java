package Control;
jajajaja
/*import java.util.ArrayList;

public class Alquiler extends Clientes{
    ArrayList<String> listado = new ArrayList<String>();
    private String matriculaVehiculo, cliente, kilometrajeInicial, kilometrajeFinal, importe;
    protected String alquiler;
    
    public void Registro(){
        System.out.print("Cliente: ");
        cliente = leer.nextLine();
        System.out.print("Teclea la matricula del auto que deseas alquilar: ");
        matriculaVehiculo = leer.nextLine();
        System.out.print("Kilometros a recorrer: ");
        kilometrajeFinal = leer.nextLine() + kilometrajeInicial;
        
        
        
    }//Registro
    
    public void Devolucion(){
        System.out.print("Cliente: ");
        cliente = leer.nextLine();
        System.out.print("Teclea la matricula del auto que deseas devolver: ");
        alquiler = leer.nextLine();
        
        for (int i = 0; i < 5; i++) {
            if(matricula[i]==alquiler){
                for (int j = 0; j < 5; j++) {
                    if(clientes[j]==cliente){
                       listado[j] = "";
                    }
                }//For
               alquilado[i] = true;
                System.out.println("Disponible");
            }
            else{
               alquilado[i] = false;
            }
        }//For
        
    }//Devolucion 
    
    public void ListadoAuto(){
        System.out.print("Teclea el modelo el vehiculo: ");
        modelo = leer.nextLine();
        
        for (int i = 0; i < 5; i++) {
            if(autos[i].equals(modelo)){
                System.out.println("Alquiler: "+listado[i]);
            }
            
        }//For
    }//ListadoAuto
    
    public void ListadoClientes(){
        System.out.print("Teclea el nombre cliente: ");
        cliente = leer.nextLine();
        
        for (int i = 0; i < 5; i++) {
            if(clientes[i].equals(cliente)){
                System.out.println("Alquiler: "+listado[i]);
            }
            
        }//For
    }//ListadoClientes
}*/
